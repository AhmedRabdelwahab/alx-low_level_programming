#include <stdio.h>
#include "main.h"

void print_alphabet(void)
{
        char letter = 97;
        for(letter;letter <= 122;letter++)

        {
                putchar(letter);
        }
                putchar('\n');



}

